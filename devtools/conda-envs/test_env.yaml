name: test
channels:
  - dglteam
  - openeye
  - conda-forge
  - omnia

dependencies:
    # Base depends
  - python
  - pip

    # Core dependencies
  - click
  - click-option-group
  - pydantic
  - pytorch
  - dgl

    # Molecule loading and processing.
  - openforcefield >= 0.8.1
  - openff-recharge >= 0.0.1a6
  - openeye-toolkits

   # Plotting
  - matplotlib

    # Testing
  - pytest
  - pytest-cov
  - codecov
