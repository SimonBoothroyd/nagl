name: test
channels:
  - dglteam
  - conda-forge
  - defaults

dependencies:
    # Base depends
  - python
  - pip

    # Core dependencies
  - click
  - click-option-group
  - pydantic
  - pytorch-cpu
  - dgl

    # SQL based molecule storage.
  - sqlalchemy
  - sqlite
  - rdkit

    # Molecule loading and processing.
  # - openff-toolkit

    # Distributed molecule processing / labelling.
  - distributed
  - dask-jobqueue

    # Python < 3.7
  - typing-extensions

    # Testing
  - pytest
  - pytest-cov
  - codecov
