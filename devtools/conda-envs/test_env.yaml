name: test
channels:
  - dglteam
  - pytorch
  - openeye
  - conda-forge
  - omnia
  - defaults

dependencies:
    # Base depends
  - python
  - pip

    # Core dependencies
  - click
  - pydantic
  - pytorch
  - pytorch-lightning
  - dgl

    # Molecule loading and processing.
  - openforcefield
  - openff-recharge
  - openeye-toolkits

   # Plotting
  - matplotlib

    # Testing
  - pytest
  - pytest-cov
  - codecov
